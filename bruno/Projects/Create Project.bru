meta {
  name: "Create Project"
  type: "http"
  seq: 2
}

post {
  url: {{base_url}}/projects
  body: json
  auth: inherit
}

body:json {
  {
    "projectName": "New Clinical Audit",
    "projectType": "PROSPECTIVE",
    "credentials": "MD",
    "status": "ACTIVE"
  }
}
